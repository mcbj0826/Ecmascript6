// Metodo Includes sirve para mirar si un valor se encuentra dentro de un arreglo.

let numbers = [1,2,3,7,8]

if (numbers.includes(9)) {
    console.log('Si se encuentra el valor ')
} else {
    console.log('Nose encuentra el valor')
}

// Como elevar a ala potencia con **.

let base = 4
let exponent = 3
let result = base ** exponent

console.log(result)
